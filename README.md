# Tabletop Framework
This project contains my C++ school project whose goal was to create a framework for creating simple 2D tabletop games. 

The final product is a C++ library which lets the user instantiate a special templated class and fill it with custom features using an event based system.

The framework also provides partial GUI support through OpenGL which allows the user to interact with (click at) game figures and other objects.  
